name: ai-agent
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.11.*
  - pip
  - numpy
  - pyyaml
  - python-dotenv
  - pydantic>=2.0
  - pip:
    - fastapi>=0.115.0
    - uvicorn[standard]>=0.34.0
    - websockets>=12.0
    - httpx>=0.28.0
    - soundfile>=0.12.0
