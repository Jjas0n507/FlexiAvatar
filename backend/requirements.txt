# AI Agent Backend — Python 依赖
# 安装: pip install -r requirements.txt
# 或使用 conda: conda env create -f ../environment.yml

# Web 框架
fastapi>=0.115.0
uvicorn[standard]>=0.34.0
websockets>=12.0

# 音频处理
numpy
soundfile>=0.12.0
PyAudio>=0.2.0

# VAD (阶段2)
# silero-vad

# ASR (阶段2)
# funasr
# torch

# TTS
edge-tts
pydub
pypinyin

# LLM
openai>=1.0.0
# anthropic  # Claude 适配器 (可选)

# 配置
pyyaml>=6.0
python-dotenv>=1.0

# 工具
httpx>=0.28.0
pydantic>=2.0

# 分词 / 音素处理 — 已在上方 TTS 区域
