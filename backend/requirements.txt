# AI Agent Backend — Python 依赖
# 安装: pip install -r requirements.txt
# 或使用 conda: conda env create -f ../environment.yml

# Web 框架
fastapi>=0.115.0
uvicorn[standard]>=0.34.0
websockets>=12.0

# 音频处理
numpy
soundfile>=0.12.0
PyAudio>=0.2.0

# VAD
silero-vad

# ASR
funasr>=1.2.0
torch

# TTS
edge-tts
# CosyVoice2（本地音色克隆）依赖不在此列 — 见 Dockerfile 固化层
# （容器方案），本地 venv 用户参考 backend/tts/cosyvoice_adapter.py 头部注释

# LLM
openai>=1.0.0
# anthropic  # Claude 适配器 (可选)

# 配置
pyyaml>=6.0
python-dotenv>=1.0

# 工具
httpx>=0.28.0
pydantic>=2.0

# 开发 / 测试
pytest>=8.0
pytest-asyncio>=0.24.0
